= form_for @professional_question do |f|
  -if @professional_question.errors.any?
    #error_explanation
      %h2= "#{pluralize(@professional_question.errors.count, "error")} prohibited this professional_question from being saved:"
      %ul
        - @professional_question.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
