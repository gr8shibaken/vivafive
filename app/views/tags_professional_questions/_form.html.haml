= form_for @tags_professional_question do |f|
  -if @tags_professional_question.errors.any?
    #error_explanation
      %h2= "#{pluralize(@tags_professional_question.errors.count, "error")} prohibited this tags_professional_question from being saved:"
      %ul
        - @tags_professional_question.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :tag_id
    = f.number_field :tag_id
  .field
    = f.label :professional_question_id
    = f.number_field :professional_question_id
  .actions
    = f.submit 'Save'
