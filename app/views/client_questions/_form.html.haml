= form_for @client_question do |f|
  -if @client_question.errors.any?
    #error_explanation
      %h2= "#{pluralize(@client_question.errors.count, "error")} prohibited this client_question from being saved:"
      %ul
        - @client_question.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
